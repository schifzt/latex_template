% \documentclass[10pt, a4paper]{jsarticle}
% \documentclass[10pt, a4paper, draft]{jsarticle}                         % disable images

\usepackage{amsmath, amssymb, mathtools}                                % greek alphabet
\allowdisplaybreaks                                                     % new page equation break control
\usepackage {autobreak}
\usepackage{bm}                                                         % math formula
\usepackage[table, xcdraw]{xcolor}                                      % color table

\usepackage[dvipdfmx]{graphicx, hyperref}
\hypersetup{                                                            % hyperref
    setpagesize=false,
    colorlinks=true,
    bookmarks=false,
    hypertexnames=true,
    allcolors=blue
}

\usepackage{pxjahyper}                                                  % bookmark index

\usepackage{tikz}
\usepackage[hang, small, bf]{caption}                                   % image multi caption
\usepackage[subrefformat=parens, font=footnotesize]{subcaption}
\captionsetup{compatibility=false}

\usepackage{ascmac}
\usepackage[english]{babel}                                             % english label, date etc
\usepackage{siunitx}                                                    % SI unit system

\usepackage{bm}                                                         % matrix vector
\usepackage{titlesec}                                                   % subsection font size

\usepackage{listings}                                                   % source code
\usepackage{color}
\definecolor{mycommentcolor}{rgb}{0,0.6,0}
\definecolor{mybgcolor}{rgb}{0.95,0.95,0.95}
\definecolor{mymauve}{rgb}{0.58,0,0.82}
\lstset{
  basicstyle={\ttfamily\footnotesize},
  columns=[l]{fullflexible},
  backgroundcolor=\color{mybgcolor},          % choose the background color
  basicstyle={\ttfamily\footnotesize},        % size of fonts used for the code
  breaklines=true,                            % automatic line breaking only at whitespace
  captionpos=b,                               % sets the caption-position to bottom
  commentstyle=\color{mycommentcolor},        % comment style
  escapeinside={\%*}{*)},                     % if you want to add LaTeX within your code
  keywordstyle=\color{blue},                  % keyword style
  stringstyle=\color{mymauve},                % string literal style
  numbers=left,
  numberstyle={\tiny \color{black}},% size of the numbers
  numbersep=9pt,xleftmargin=2em
}

\pagestyle{headings}                                                    % page number on/off
\usepackage[T1]{fontenc}                                                % avoid mojibake
\usepackage{latexsym}                                                   % symbol extension

% \usepackage[noadjust]{cite}    